# Icons (placeholder)

The module CMakeLists references these PNG/GIF assets (same set as
DentalSegmentator). They are **not yet created** — add the real artwork before
submitting to the Slicer Extensions Index:

| File | Use |
|---|---|
| `BreastMRISegmentator.png` | Module icon (small, square) |
| `BreastMRISegmentator_full_icon.png` | Extension icon (Extensions Manager) |
| `info.png` | Info button glyph |
| `loading.gif` | Spinner shown while inference runs |
| `start_icon.png` | Apply-button glyph |

Until the real icons exist, the module still loads; Slicer falls back to a
default icon. Keep these square (e.g. 128×128) and use the label palette
(breast = teal `#4FB0C6`, FGT = amber `#E6B84F`, tumor = red `#D8654F`).
